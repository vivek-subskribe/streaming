**Sample setup for streaming endpoints**

Spin up the application and go to `localhost:9090/api/stream` on the browser or try `curl -H "Accept: text/event-stream, application/json" http://localhost:9090/api/stream`
